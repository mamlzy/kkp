services:
  # MySQL Database
  # db:
  #   image: mysql:8.0
  #   container_name: kkp-db
  #   restart: unless-stopped
  #   environment:
  #     MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD:-root}
  #     MYSQL_DATABASE: ${MYSQL_DATABASE:-kkp}
  #   volumes:
  #     - mysql_data:/var/lib/mysql
  #     - ./init.sql:/docker-entrypoint-initdb.d/init.sql:ro
  #   healthcheck:
  #     test: ['CMD', 'mysqladmin', 'ping', '-h', 'localhost']
  #     interval: 10s
  #     timeout: 20s
  #     retries: 10
  #     start_period: 30s

  # Backend FastAPI
  kkp-api:
    container_name: kkp-api
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - '5000:5000'
    volumes:
      - backend_models:/app/app/models
      - backend_uploads:/app/app/uploads

  # Frontend
  kkp-web:
    container_name: kkp-web
    build:
      context: ./frontend
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - '5173:5173'

volumes:
  mysql_data:
  backend_models:
  backend_uploads:
